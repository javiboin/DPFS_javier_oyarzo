<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('./partials/head') %>
    <link rel="stylesheet" href="/stylesheets/index.css">
</head>
<body>
    <%- include('./partials/header') %>

    <%- include('./partials/nav') %>

    <main>

        <h2>Productos</h2>

        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/">Home</a></li>
            <li class="breadcrumb-item"><a href="/">Productos</a></li>
        </ol>
        
        <div class="container">

        <% for (i=0; i < products.length; i++) { %>
            <div class="card">
                <a href="/products/product-detail/<%= products[i].id %>"><img src="/images/<%= products[i].image %>" class="card-img-top" alt="Imagen temporal"></a>
                <div class="card-body">
                    <h5 style="margin-bottom: 10px;"><%= products[i].name %></h5>
                    <p class="price"><b><%= products[i].price %></b></p>
                    <p style="color: grey; font-size: 14px;"><%= products[i].price_cash %> con Transferencia o depósito</p>
                    <p style="color: red; font-size: 12px;"><%= products[i].price_installment_count %> Coutas de <%= products[i].price_installment %></p>
                    <button><a href="/products/product-cart">Comprar</a></button>
                </div>
            </div>
        <% } %>

        </div>
    </main>

    <%- include('./partials/footer') %>
</body>
</html>
