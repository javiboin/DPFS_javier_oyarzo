<nav class="navbar navbar-expand-lg bg-body-tertiary bg-dark" data-bs-theme="dark">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse menu-h" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <p>Categorías</p>
                    <ul class="menu-v">
                        <% categories.forEach(category => { %>
                            <li><a href="/products/search?q=<%= category.name %>"><%= category.name %></a></li>
                        <% }) %>
                    </ul>
                </li>
                <li class="nav-item">
                    <p>Marcas</p>
                    <ul class="menu-v">
                        <% brands.forEach(brand => { %>
                            <li><a href="/products/search?q=<%= brand.name %>"><%= brand.name %></a></li>
                        <% }) %>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>

<% if (currentUser) { %>
    <br>
    <p id="userName"><b>¡Bienvenido <%= currentUser.firstName %>, <%= currentUser.lastName %>!</b></p>
<% } %>
